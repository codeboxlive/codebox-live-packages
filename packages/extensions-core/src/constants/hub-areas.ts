import { HubArea } from "@codeboxlive/window-messaging";
import { FluidRequests } from "../fluid";

export const FLUID_HUB_AREAS = new FluidRequests();

export const ALL_HUB_AREAS: HubArea[] = [FLUID_HUB_AREAS];
